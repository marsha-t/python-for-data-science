def count_in_list(lst, item):
    """
    Count frequency of item in list

    Args:
        lst (list):
            list to search in
        item:
            item to count frequency for

    Returns:
        int:
            frequency of item in list
    """
    return lst.count(item)
